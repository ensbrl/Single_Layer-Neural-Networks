#pragma once
#include <math.h>
#include "Matris.h"

int Ypoint(int x, Matrix^ w, int Carpan);
double net(Matrix^ oMatrix, Matrix^ x);
double activationFunction(double net);
double turev(double fnet);



